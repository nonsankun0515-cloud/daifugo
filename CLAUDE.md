# Cards Table（大富豪・七並べ・スピード）— 引き継ぎメモ

本格的なトランプゲームのアプリ。アプリ名は **Cards Table**（2026-09-26 にユーザーが決めた。ページの `<title>`・ホーム画面の名前・manifest）。リポジトリ名・URL・保存のキー（`daifugo.*`）は「大富豪」だった頃のまま変えない（ホーム画面のアプリや招待リンク、保存データが切れるため）。
**下のナビ**で「大富豪・七並べ・スピード・マイページ」を切り替える。
どのゲームも AIロボットとの対戦（レート戦／フリー対戦）、友だちとのオンライン対戦（チャット付き）、対局中でも開けるルールブック、画面ごとのBGMがある。
iPhoneのホーム画面に置けるアプリ版がある。今後もゲームを1つずつ足していく予定（候補：ポーカー、ババ抜き、ダウト、神経衰弱、ブラックジャック）。
新しいチャットで続きをするときは、まずこのファイルを読むこと。

## 公開先

| もの | 場所 |
|---|---|
| アプリ版（iPhone用・オンライン対戦あり） | https://nonsankun0515-cloud.github.io/daifugo/ （GitHub Pages、`main` の `/docs`） |
| リポジトリ（公開） | https://github.com/nonsankun0515-cloud/daifugo |
| Claude上のページ（本人専用・オンラインは使えない） | https://claude.ai/artifact/6N9oucWaN8P6Mr73hEU3Gb |
| オンライン対戦サーバー | `wss://daifugo-nonsankun0515.daifugo-nonsankun0515.workers.dev`（Cloudflare Workers + Durable Objects、無料枠） |

## 進め方の方針（ユーザーの希望）

- **追加料金は一切かけない**（無料枠だけ）。ユーザーにしてもらう作業は「サインイン」だけにする。
- 画面の言葉は日本語。見た目は本格的なカジノテーブル風（チープにしない）。アニメーションは好評。
- ルールの意味があいまいなときは実装前に確認する。音声入力なので聞き間違いがある（「Q（キュー）」が「9」になるなど）。
- 大きな方針（新しいサービス、ダウンロード、公開範囲など）は相談してから決める。

## マイルール（ユーザーが決めたルール。プリセット `mine`）

| カード | ルール | 内容 |
|---|---|---|
| ♠3 | スペ3返し | ジョーカー1枚に♠3を出せる。場が流れて♠3の人が親 |
| 3×2 | 3止め | 1枚の5スキップ・13スキップを止める。**飛ばされる本人だけ**が出せる |
| 3×3 | 砂嵐 | 自分の番ならどんな場にも出せる最強の役（場が流れる）。8切り・ろくろ首・救急車は誰でも止められる。スキップを止められるのは飛ばされる本人だけ |
| 4×2 | 4止め | 1枚の8切りを止める |
| 5・K | 5スキップ・13スキップ | 出した枚数ぶん次の人を飛ばす |
| 6 | 6→9 | 1枚の6の次は9しか出せない（強さは無関係） |
| 6×2 | ろくろ首 | 8切りと同じ。止められるのは砂嵐だけ |
| 7 | 7渡し | 出した枚数まで次の人に手札を渡せる |
| 8 | 8切り | 何枚でも場が流れる（階段に8が入っていても） |
| 9 | 9→6 | 1枚の9の次は6しか出せない |
| 9×2 | 救急車 | ろくろ首と同じ（2026-09-25 に追加） |
| 10 | 10捨て | 出した枚数まで手札を捨てられる |
| J | Jバック | 場が流れるまで強さが逆転（一時的な革命） |
| Q | 12ボンバー | Qの枚数ぶん、**自分の手札にある数字**を指名。全員その数字を全部捨てる。指名しないのも可 |
| A | A拾い | 1つ前に出されたカードを手札にできる（取るかは自由） |
| JOKER | ワイルド | 好きなカードの代わり。1枚出しは2より強い |
| 上がり | 禁止上がり | ジョーカー・2・革命中（Jバック中）の3で上がると最下位 |
| しばり | スート縛り・片縛り・階段縛り・激縛り | 5♠→6♠なら次は7♠だけ、Q♥Q♦→K♥K♦なら次はA♥A♦だけ |

- 止め札（3止め・4止め・砂嵐）で止めたら、場が流れて止めた人が親になる。
- ほかのローカルルール（革命・都落ち・下剋上・天変地異・クーデター・オーメン・大革命・ラッキーセブン・9リバース・9戻し・ダウンナンバー・2バック・階段革命など）も画面で1つずつ切り替えられる。
- ジョーカーを含む出し方では新しい縛りは発生しない（縛り中は縛りのマークとして出せる）。
- `MINE` を変えたときは、変更前の内容を `js/rules.js` の `OLD_MINES` に追加する（保存済みの設定と途中の対局を `RU.migrate()` が新しいマイルールに更新する）。

## 得点とレート戦（2026-09-26 にユーザーと決めた）

- **1ゲームの得点**：大富豪 +3・富豪 +1・平民 0・貧民 −1・大貧民 −3（3人は +3/0/−3、5人 +3/+1/0/−1/−3、6人 +3/+1/0/0/−1/−3）。`E.pointsFor`。
- **試合**：`createMatch({ games })` でゲーム数を決めると、そのゲーム数で `S.matchOver`。`S.history` にゲームごとの得点。フリー対戦は 5／10／無制限、レート戦は10ゲーム固定。
- **レート戦**：**4人・マイルール・10ゲームで固定**（ユーザーの指定）。はじめは1500。`js/rating.js`：
  - 相手それぞれに勝つ確率（Elo、400差で約91%）から順位の分布を出し「予想の総得点」を計算。
  - 増減 = K ×（総得点 − 予想）÷ 30。K=40、はじめの5試合は80。
  - AIのレートは固定：やさしい1310・ふつう1500・つよい1630（`tools/calibrate.js` で実測。ふつう=1500基準）。
  - 棄権（途中でやめる）は残りのゲームを全部 大貧民（−3）として計算。タイトルに戻るだけなら棄権ではない（つづきから）。
- **AI戦のレート**は端末の localStorage（`store.rating`）。最終ゲームが終わった瞬間に `applyRated()`（`S.rated.result` で1回だけ）。
- **オンラインのレート**は AI戦と別。サーバーの Durable Object `Ratings`（1つだけ、`idFromName('global')`）に、再接続用ID `cid` の SHA-256 ハッシュをキーに保存（IDそのものは保存しない）。部屋が `ratingOut` に `{cid, delta}` をためて、サーバーが `record()` で保存。
  - 最初から座っていた人（`seat.ratedCid`）だけが対象。途中で部屋を出た人は棄権。途中参加は対象外（`view.youRated`）。
  - 試合が終わったら `{t:'lobby'}` で待合室に戻る（レート戦は最後まで終わってから）。
- **ルールブック**（`js/book.js`）：遊び方・ルール（数字ごとの効果をカードの絵つきで）・得点とレート・用語。タイトル、対局中の上のボタン、メニューから開ける。開いていても対局は進む。対局中は「このゲーム／レート戦」、タイトルでは「レート戦／フリー対戦」のルールを切り替えて見られる。

## 七並べ（2026-09-26 にユーザーと決めた）

- 7は配った直後に自動で全部場に置き、♦7を持っていた人から。場に並んだカードの**となり**に1枚出す（失格者のカードが飛び地に並んでも、そのとなりに出せる）。
- 最初からオンのルール（=レート戦の「標準ルール」）：**パス3回まで**（4回目で失格。手札は場に並べ最下位）・**ジョーカー1枚**・**トンネル**（AとKがつながる）。ほかに「出せるときはパス禁止」も切り替えられる。
- ジョーカー：出せる場所ならどこにでも置ける（自分のカードの場所は不可）。その場所のカードの持ち主は次の番で必ずそれを出し、ジョーカーを受け取る（その番はパス不可）。
- パス無制限のときは、全員が2周続けてパスしたら、出せる人は出さなければならない（止まり続けないように）。
- 得点は大富豪と同じ（順位で +3/+1/−1/−3）。順位：上がった順 → 最後の1人 → 失格（後に失格した人ほど上）。
- レート戦：4人・標準ルール・10ゲーム。AIのレート：やさしい1430・ふつう1500・つよい1590（運の要素が大きく差は小さい。`tools/calibrate_sevens.js` を数十ゲームずつ14本並列で）。
- AIの「ふつう」は自己対戦で重みを調整（`D.SevensAI.W`）。つよい＝見えないカードを配り直して最後まで試すモンテカルロ。2026-09-26 に改良：候補の手（最大6つ、パスは必ず入れる）を**同じ配り方・同じ乱数で比べる**（以前は手ごとに別の配り方で、運の差に埋もれていた）／試す回数は**300回で固定**（`SevensAI.ITERS`。時間で決めると遅い端末ほど弱くなりレートがずれる。3秒は上限）／評価関数を配列を作らない形にして約1.5倍速く（ふつうの判断は6万通り以上で以前と完全に同じことを確認）。PCで1手0.5〜1秒。

## スピード（2026-09-26 にユーザーと決めた）

- 2人。席0が赤（♥♦）、席1が黒（♠♣）の26枚ずつ。場札4枚・山札22枚。「せーの」で山札から台札に1枚ずつ（アプリでは自動）。
- 台札より**1つ大きいか小さい数字**、**同じ数字**も出せる、**AとKがつながる**（ユーザーの指定。どれもルールで切り替え可）。順番なし・早い者勝ち。出したら山札から補充。
- 2人とも出せなくなったら「せーの」（山札がない人は場札から）。先に全部出した方の勝ち。**同時になくなったら引き分けでそのゲームは数えない**（AI同士だと約9%。ユーザーは「人間同士ならほぼ起きない」のでこのままと決めた）。
- オンラインで**通信が30秒切れたままの人は、そのゲームの負け**（相手の人がつながっているときだけ。ユーザーが決めた。`online-room.js` の `speedAway`・`AWAY_FORFEIT`、エンジンは `Speed.forfeit`、`S.endReason='away'`）。スピードは人の席にAIは入らない（以前は切れた人がいると止まったままになることがあった）。
- 試合：1ゲーム／3本勝負／5本勝負。レート戦は3本勝負（2勝で勝ち）。レートは勝ち負けのイロレーティング：増減 = 32 ×（勝ち1/負け0 − 勝つ確率）、はじめの5試合は64。
- AIの強さ＝出すまでの反応時間（やさしい1.5〜2.3秒・ふつう0.95〜1.4秒・つよい0.56〜0.85秒）＋つよいは相手を止める手を選ぶ。AIのレート：1250・1500・1850（AI同士のシミュレーションで実測）。
- 時間の進行は `js/speed/host.js`（`S.clock` に予定時刻。画面とサーバーで共通）。操作は**ドラッグだけ**（2026-09-26 ユーザーの希望「タップだけだとつまらない」。レート戦・オンラインも同じ）：つかむと出せる台札が光る（`.spd-pile.can`）、カードは指に吸いつくように動き、カードの真ん中が台札の上（少し広め）なら出す（`dropTarget`）。台札に向かって速くはじいても出せる（離す直前110msの速さ0.45px/ms以上・向きの差32度以内：`flickTarget`）。タップだけなら案内のトースト（8秒に1回）。出せなければ元の位置に戻って首を振る（`snapBack`）。出したカードは離した場所から台札へ飛ぶ。
- オンライン：サーバーが正。自分の手はすぐ動かして見せ（`pending`）、サーバーの結果で合わせる。相手が先に出して間に合わなかったら「相手が先に出しました」。

## ファイル

| ファイル | 役割 |
|---|---|
| `index.html`, `css/style.css` | 画面（開発用。ブラウザでそのまま開ける）。ホーム（`scr-home`＋下のナビ）・各ゲームの卓・設定・オンライン |
| `js/main.js` | 起動（共通部品→各ゲームの `init`→最後のタブ、または招待された部屋） |
| `js/shell.js` | 外枠：下のナビ・ゲームのホーム（`homeHTML`）・マイページ（名前・全ゲームのレート・共通設定） |
| `js/common.js` | 共通：保存（`localStorage` の `daifugo.v1`。`store.sevens`/`store.speed` にゲームごと）・個人設定・画面切り替え・設定の部品・レート戦（`applyRatedLocal` 等）・試合結果・ルールブックの枠 |
| `js/install.js` | ホーム画面に追加の案内（ブラウザを見分けて手順を出す。Android・PCの Chrome / Edge は `beforeinstallprompt` で「追加する」。ホーム上の案内は閉じたら30日出さない：`store.installTipOff`） |
| `js/bgm.js` | BGM（画面ごとに曲を替える。音量は Web Audio の GainNode。`D.BGM.status()` で様子を見られる） |
| `audio/` | BGM の mp3（96kbps）と `CREDITS.txt`。`build.py` が `docs/audio/` にコピー |
| `js/online-client.js` | オンラインの画面側（接続・待合室・チャット・招待）。対局中は `D.Games[game].online` に任せる |
| `js/sevens/` | 七並べ：`engine.js`（ルール）・`ai.js`・`ui.js`（ホーム・設定・卓・オンライン）・`book.js` |
| `js/speed/` | スピード：`engine.js`・`ai.js`・`host.js`（時計）・`ui.js`・`book.js` |
| `js/cards.js` | カードの定義・並べ替え・乱数 |
| `js/rules.js` | ルールの一覧・説明・プリセット・`migrate` |
| `js/engine.js` | **ルール判定の本体**。画面から独立した状態機械（サーバーでも同じものを使う）。得点表・試合の長さもここ |
| `js/rating.js` | レート戦の計算（予想の総得点・増減・棄権・AIのレート）。ブラウザ・サーバー・テストで共通 |
| `js/ai.js` | AI。やさしい・ふつう＝評価関数、つよい＝相手の手札を仮定して何百回も試す（モンテカルロ） |
| `js/art.js` | カード・ロボット・アイコンのSVG |
| `js/textures.js` | テーブルとロビーの素材（フェルト・木・革・絨毯）をキャンバスで作る。ボタン・板の額縁（SVG）もここ |
| `js/sound.js` | 効果音（Web Audioで合成） |
| `js/ui.js` | テーブルの描画と演出（エンジンのイベントを順に再生）。`fitCards()` でカードの大きさを画面に合わせる。出したカードの履歴は持たない（カウンティングさせないため。場の上の3組だけ見える） |
| `js/book.js` | 大富豪のルールブックの中身（`D.Books.daifugo`）。枠は `common.js` |
| `js/net.js` | オンライン対戦の通信（自動で再接続） |
| `js/app.js` | 大富豪のホーム・設定・ローカル対戦の進行・オンライン対戦の卓（`D.Games.daifugo`） |
| `js/online-room.js` | 部屋の仕組み `RoomCore`（サーバーとテストで共通）。ゲームごとの違いは `ADAPTERS`（大富豪・七並べ＝順番のあるゲーム、スピード＝リアルタイム） |
| `server/` | Cloudflare のサーバー（`wrangler.toml`, `src/index.js`）。`js/` のエンジンをそのまま読み込む。Durable Object は部屋ごとの `Room` と、レート保存用の `Ratings` |
| `tests/test.html` | 大富豪のルール60件＋オンライン11件＋七並べ・スピード15件（`games_tests.js`）＋AI のテスト |
| `tools/run_tests.js` | 上と同じテストをブラウザなしで実行（`node tools/run_tests.js`、約30秒。失敗があれば終了コード1） |
| `tests/bench.html` | AIの強さ比べ（`?games=60&budget=300`） |
| `tests/frame.html` | 大きな画面の見た目確認（`?w=1366&h=768`。Browserペインが小さいため） |
| `tools/build.py` | 1ファイルにまとめる → `dist/index.html`, `dist/artifact.html`, `docs/`（アプリ版） |
| `tools/devserver.py` | 開発用サーバー（キャッシュさせない） |
| `tools/calibrate.js` | 大富豪のAIのレートを実測（`node tools/calibrate.js easy 4000`／`hard 40 420` を並列で） |
| `tools/calibrate_sevens.js` | 七並べのAIのレートを実測 |
| `tools/make_icons.py` | ホーム画面用アイコン（Pillow） |
| `tools/reencode_bgm.mjs` | BGM を 96kbps の mp3 に変換（使い方はファイルの先頭。npm の mpg123-decoder と lamejs を作業用フォルダに入れて使う） |
| `docs/` | 公開中のアプリ版（ビルドで作る。手で編集しない） |

## 仕組みのポイント

- **エンジン**：`E.getRequest(S)` で「次に誰が何をするか」、`E.apply(S, action)` で進めてイベント列を返す。UIはイベントを順に再生し、最後にエンジンの状態と合わせる。
- **オンライン**：サーバーが正（ルール判定・AI・保存）。`RoomCore.viewFor()` が各人に「自分の手札だけ見える状態」と要求を送る。7渡し・交換のカードは当事者だけに見せる。AIの手番はDurable Objectのアラームで進める。切断した人の番は15秒後にAIが代打ち。退出した席はAIが引き継ぎ、途中参加はAIの席に座る。招待リンクは `…/daifugo/#r-部屋コード`。
- **ホーム画面に追加**（2026-09-26 ユーザーの希望「Chromeで開いても追加できるように」）：iPhone は Safari・Chrome・Edge・Firefox どれでも共有ボタン →「ホーム画面に追加」。LINE・Instagram などアプリの中のブラウザでは追加できないので、ふつうのブラウザで開き直す案内（LINE は `?openExternalBrowser=1` で外のブラウザが開く）。招待リンクにも `?openExternalBrowser=1` を付け、開いたあと `Install.cleanURL()` でアドレスから消す。manifest に `id: "./"`。ホーム画面のアプリの localStorage はブラウザとは別（案内にも書いた）。
- **BGM**（2026-09-26 ユーザーの希望。**最初から小さめの音で鳴る**）：曲は **Kevin MacLeod（incompetech.com）、CC BY 4.0**。ホーム・設定・待合室＝Lobby Time、大富豪＝Cool Vibes、七並べ＝Bossa Antigua、スピード＝Hep Cats（ユーザーが選んだ）。クレジットはマイページの下・README・`audio/CREDITS.txt`（CC BY の決まり：曲名・作曲者・ライセンス・変換したこと）。
  - DOVA-SYNDROME は 2026-09-15 に **OpenTracks** に名前が変わり、規約で「利用者が音声ファイルに簡単にアクセス・複製できる状態での利用」を禁止している → 公開リポジトリに置けないので使わない。曲を足すときも CC BY / CC0 など再配布できるものにする。
  - 仕組み：audio は1つだけで曲（src）を入れ替える（iPhone は最初のタップで再生した audio しか後から鳴らせない）。最初の `pointerdown`／`keydown` で鳴らし始める。音量は GainNode（iPhone は `audio.volume` が効かない）：小0.16・中0.3・大0.5。曲を替えるときは0.6秒で下げてから。アプリを離れたら止める。画面が変わると `CM.onShow` → `BGM.sync()`。
  - 設定：`settings.bgm`（オン・オフ）・`settings.bgmVol`（low/mid/high）をマイページで。対局中のスピーカーのボタンは**効果音と BGM をまとめて**消す／出す（`CM.toggleSound`）。
  - service worker は mp3 を保存しない（Range で少しずつ読むため）。Claude上のページには `files` で `audio/*.mp3` も一緒に公開している。
- **ロビーの背景**（2026-09-26 ユーザーの希望「茶色一色だとダサい、カジノ風に」。5案（絨毯・ポーカールーム・アールデコ・ラスベガスの夜・VIPルーム）から**カジノの絨毯**を選んだ）：ワインレッドの地に金の菱形の格子と花のメダリオン。模様は `textures.js` の `carpet()`（画像ファイルなし、2倍で描いて `--tex-carpet`）。色は `:root` の `--lb-room`（背景）・`--lb-panel`（タイル・名前の板）・`--lb-strip`（レートの帯）・`--lb-bar`（下のナビ）・`--lb-card`（設定の板）・`--lb-line`（金の枠線）にまとめた。ホーム・マイページ・ルールと設定・オンラインの画面に使う（対局中の卓・ダイアログ・ルールブックは前のまま）。模様の上にじかに載る文字（見出し・注意書き・クレジット）には黒い影を付けて読みやすくしている。
- **ヨーロッパ調のヴィンテージの見た目**（2026-09-27 ユーザーの希望「全体的に丸っこくてAIっぽい。字体を華やかに。ボタンの枠をヨーロッパ調のヴィンテージフレームに」）：
  - **丸いカプセル形・角丸はやめた**。ボタンは角を内側にえぐったラベル形の板（`.btn-gold`＝金の板、`.btn-ghost`＝黒檀の板に金の二重線）、タイル・設定の板・レートの帯・トースト・アイコンのボタンは金の二重線の額縁と四隅の飾り石、見出しの緑の板・ダイアログ・部屋コードの板は角に唐草の大きな額縁。トランプのカード・卓・アバターのメダルは本物と同じく丸いまま。
  - 額縁は `textures.js` の `frameGold/frameDark/framePanel/frameOrnate` が SVG を作り、CSS 変数 `--fr-gold/--fr-dark/--fr-panel/--fr-orn` に入れる。CSS は `border: 8px solid transparent; border-image: var(--fr-panel) 16 / 8px stretch;`（ボタンは `16 fill`。切り分け16、見出し・ダイアログは32）。9つに切って伸ばすので、角の飾りは切り分けの内側、辺はまっすぐな線だけにする。
  - 額縁を付けた板は `background-origin: border-box`（CSS の最後にまとめてある。付けないと枠の部分に背景の繰り返しの帯が出る）。ボタンの背景は額縁が出ないときの控えで `padding-box`（えぐった角に出ないように）。影は `box-shadow` ではなく `filter: drop-shadow`（えぐった形に沿う）。
  - **字体**（ユーザーが4つから選んだ）：見出し・ボタン・タイル・下のナビ・切り替えは **Kaisei Decol**（`--font-display`。伸ばす棒「ー」が波の形になる装飾のある明朝）、英字と数字（レート・部屋コード・得点）は **Cinzel**（`--font-num`）、「DAIFUGŌ」などの英字の副題は **Cinzel Decorative**（`--font-latin`）、説明の文は今までどおり Zen Kaku Gothic New（`--font-ui`）。カードの数字は Playfair Display のまま（本物のトランプの印刷なので）。卓に金色で印刷する文字（`ui.js` の `renderFeltPrint`）も Kaisei Decol。
  - 入力欄は iPhone の角丸を消すために `-webkit-appearance: none; border-radius: 0`。
- **チャット**：部屋の人だけに届く。60文字まで・10秒に6通まで・サーバーは直近30件だけ保存（入室時に `chatlog` で渡す）。チャットでは公開ID `pid` を使い、再接続用の `cid` は他人に見せない。対局中は送った人の席の上に吹き出し（クラスは `.bubble.talk`。`.chat` はチャット欄なので使わない）。
- **サーバーのAI**は「ふつう」まで（無料枠のCPU時間の上限のため）。
- **Claude上のページ**はWebSocketが使えないので、オンライン対戦のボタンを隠している（`Net.available()`）。
- **ゲームの足し方**：`js/<game>/` に engine（`createMatch/startGame/getRequest/apply/serialize/deserialize`）・ai・ui・book を作り、`D.Games[id] = { init, renderHome, bookSources, online, state, stopLocal }` を登録。`shell.js` の `TABS`、`rating.js` の `CFG`、`online-room.js` の `ADAPTERS`、`online-client.js` の `LOBBY`、サーバーの import と `Ratings` の `GAMES` に1行ずつ足す。
- **部屋のゲーム**：最初に入った人の `hello.game` で決まる（空の部屋のとき）。招待リンクはゲームを問わず `#r-コード`。オンラインのレートは `Ratings` にゲームごと（大富豪は以前のキーのまま、ほかは `:sevens` などを付ける）。

## 開発のしかた

- 手元で動かす：`.claude/launch.json` の **daifugo**（`py tools/devserver.py 8793`）→ http://localhost:8793/
- 手元のオンラインサーバー：**daifugo-server**（`wrangler dev`、ポート8787）。localhost で開くとアプリは自動でこちらにつながる。
- 2人で試す：http://localhost:8793 と http://127.0.0.1:8793 を別タブで開く（別の端末として扱われる）。
- テスト：`node tools/run_tests.js`（PATH の先頭に Node のフォルダを入れる）か http://localhost:8793/tests/test.html。**全部合格してから公開**する。
- 別のチャットの開発サーバーが 8793 で動いたままのことがある。そのときは preview_start が使えないが、Browserペインで http://localhost:8793/ を直接開けば同じファイルが見える（キャッシュしないサーバー）。
- ハッシュ（`#r-…`）だけ違うURLへの移動ではページが再読み込みされない。コードを変えたら `location.reload()` する。
- **見た目をまとめて確かめる**：Browserペインのスクリーンショットは失敗しやすい（隠れているとき・画面の切り替え直後）。Edge のヘッドレスなら確実に撮れる（ユーザーに見せる画像は `--force-device-scale-factor=2` でスマホと同じ細かさに）：PowerShell で `Start-Process msedge.exe -ArgumentList '--headless=new','--disable-gpu','--hide-scrollbars','--user-data-dir=<作業用フォルダ>','--window-size=2440,880','--virtual-time-budget=8000','--screenshot=<出力.png>','http://localhost:8793/…' -Wait`（Edge は `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`。出力先は Windows 形式のパス）。いくつかの画面を並べるときは、同じオリジンの確認用ページに 375×812 の iframe を並べ、親から `localStorage`（`installTipOff` など）を書いてから読み込み、`iframe.contentWindow.DFG…` やボタンのクリックで画面を切り替える（確認用ページはコミットしない）。ヘッドレスでは演出が途中で止まって半透明に写るので、iframe に `*{animation-duration:0s!important;transition:none!important}` を足し、`Element.prototype.animate` を空にしてから撮る。字体の候補を比べるときは、iframe の head に Google Fonts の link と `:root{--font-display:…}` を足せばアプリを変えずに試せる。

## 公開のしかた

1. `py tools/build.py`
2. テストが全部合格することを確認（`node tools/run_tests.js`）
3. `git add -A` → `git commit`（末尾に `Co-Authored-By: Claude …`）→ `git push` → GitHub Pages が1〜2分で更新
   - 確認：`C:\Users\Nozo3\gh-cli\bin\gh.exe api repos/nonsankun0515-cloud/daifugo/pages/builds/latest`
4. サーバーを変えたとき：`server` フォルダで、PATH の先頭に `C:\Users\Nozo3\node\node-v24.21.0-win-x64` を入れてから `npx wrangler deploy`
   - 新しいメッセージの種類を足すときは**サーバーを先に**公開する（古いアプリは知らない種類を無視する）
5. Claude上のページ：`dist/artifact.html` を上のartifactのURLに再公開する

## このPCの道具

- GitHub CLI：`C:\Users\Nozo3\gh-cli\bin\gh.exe`（`nonsankun0515-cloud` でサインイン済み）。リポジトリのgit設定は、公開用の非公開メールアドレスと、gh を使った認証。
- Node.js（ポータブル）：`C:\Users\Nozo3\node\node-v24.21.0-win-x64`
- wrangler：`server/node_modules`（Cloudflareにサインイン済み、権限はWorkersの公開に必要な分だけ）
- Python 3.12：`py`（Pillow あり）
- 認証トークンは表示・保存・送信しないこと。

## ハマったところ

- 部品が `display` を指定しているので、`[hidden]{display:none!important}` が必要。
- スピードの台札のカードの `z-index` は「見えている3枚の中の順番」で付ける（通し番号にすると、カードが増えたとき「せーの」の表示が隠れた）。
- 七並べの操作ボタンは幅375pxで3つが1段に収まるぎりぎり。ラベルを長くすると「出す」が2段目に落ちる（パスの残り回数は `<small class="sub">` で小さく）。
- 大富豪の卓が隠れて大きさ0のときにフェルトの印刷を描くと、楕円の半径が負になってエラーが出た（`renderFeltPrint` で幅が小さいときは描かない）。
- Bash の `rm` でスクラッチのフォルダを消そうとすると安全チェックで止められる。測定の出力は毎回新しいフォルダ名にする。
- **Browserペインが画面の後ろに隠れていると**（`document.visibilityState === 'hidden'`）アニメーションが進まず、大富豪の配る演出などで止まって見える。動作確認は `Element.prototype.animate = () => ({ finished: Promise.resolve(), cancel(){} })` で演出を飛ばしてから。スクリーンショットも失敗しやすい。
- Browserペインで別のサイト（github.io など）を開いたタブは、その後 javascript_tool がページの変数（`DFG`）を見られなくなることがある → 新しいタブ（`tabs_create`）で localhost を開き直す。
- 合成イベントで BGM を鳴らし始めるテストは `document.body.dispatchEvent(new PointerEvent('pointerdown',{bubbles:true}))`（document 自体に投げると反応しなかった）。
- 大富豪の「履歴」はカウンティング防止で**わざとなくした**（ユーザーの希望）。戻さないこと。
- クラス名 `.me` は自分の手札の欄（display:flex）。表の行などに `me` を付けると崩れる（得点表は `mine`）。
- style属性に埋め込む背景画像は `url('…')`（シングルクォート）。
- Workers では読み込み時の非同期処理・乱数が禁止 → ファイルの一番外側で `MessageChannel` や `Math.random` を使わない（`ai.js` の yield は遅延作成）。
- workers.dev のサブドメインは `package.json` の名前から自動登録された（`daifugo-online` は他人が使用中だった）。

## これまでの経緯

- 2026-09-24：初版（ルールエンジン・全ローカルルール・AI・SVGカード・演出・効果音）
- 2026-09-25：12ボンバーは自分の手札の数字だけに／3止めは飛ばされた人だけに／カードを大きく／テーブルを刷新（革・木・フェルト・金の印刷）／iPhoneアプリ版（GitHub Pages、PWA）／救急車をマイルールに追加
- 2026-09-26：オンライン対戦（Cloudflare）／チャット
- 2026-09-26（2）：得点表（+3/+1/−1/−3）・1試合10ゲーム・レート戦（AI戦とオンライン、別々のレート）・ルールブック
- 2026-09-26（3）：複数ゲームのアプリに（下のナビ・ゲームごとのホーム・マイページ）／七並べ・スピードを追加（AI戦・レート戦・オンライン・ルールブック）
- 2026-09-26（4）：アプリ名を「Cards Table」に／七並べの「つよい」を改良（+48→+91、レート1590）／オンラインのスピードは30秒切断で負け／不具合修正（スピードの「せーの」が台札に隠れる・七並べの「出す」が2段目に落ちる・大富豪のフェルト印刷のエラー）／スピードの途中保存／古いタイトル画面のCSSを削除／テストをNodeで実行できるように（`tools/run_tests.js`）
- 2026-09-26（5）：ホーム画面に追加の案内（ブラウザごとの手順・Chrome / Edge の「追加する」・LINE から外のブラウザで開く招待リンク）
- 2026-09-26（6）：スピードをドラッグで出す方式に（タップでは出さない・はじいても出せる・出せる台札が光る）
- 2026-09-26（8）：BGM（Kevin MacLeod の4曲・画面ごと・小さめの音で最初から・マイページで設定・スピーカーのボタンでまとめて消す）
- 2026-09-26（9）：ロビーの背景をカジノの絨毯に（茶色一色をやめた。ホーム・マイページ・設定・オンラインの画面。タイル・レートの帯・下のナビもワインレッドに）
- 2026-09-27（10）：ヨーロッパ調のヴィンテージの見た目に（丸いボタン・角丸をやめて額縁に。字体を Kaisei Decol・Cinzel に。ダイアログ・ルールブック・チャット欄も茶色からワインレッドに）
- 2026-09-26（7）：大富豪の「履歴」（出したカードの一覧）をなくした（ユーザーの希望：カウンティングできないように）。ボタン・画面・保存（`store.log`）とも削除。12ボンバーで指名しなかったときは吹き出しで知らせる

## 現在の状態（2026-09-27、5つ目のチャット。次のチャットはここから）

- **すべて公開済み**：サーバー（Workers。スピードの30秒切断のルール入り。このチャットでは変更なし）・アプリ版（GitHub Pages）・Claude上のページ（バージョン14、BGMの mp3 つき）。この上の（10）のヴィンテージの見た目まで。
- **テスト**：`node tools/run_tests.js` で 大富豪60・オンライン11・七並べ／スピード15・AI 5 すべて合格。
- **確かめたこと**：スマホ幅での各ゲームの卓・七並べのボタンが1段・スピードの「せーの」の表示／スピードのドラッグ（タップでは出ない・正しい台札に落とすと出る・出せない台札なら戻る・はじいて出る・ゆっくり途中で離すと戻る）／ホーム画面に追加の手順（Android Chrome・iPhone Chrome・LINE の表示）と本番の PWA の条件／大富豪を履歴なしで8手遊んでエラーなし／BGM が画面ごとに替わる・オン／オフ・スピーカーのボタン・クレジット表示／手元サーバーで、スピードの部屋から1人が切断すると30.4秒後にその人の負け／ロビーの絨毯（3つのゲームのホーム・マイページ・ルールと設定・オンラインの入口・レート戦のダイアログ）をスマホ幅で撮って、文字が読めること／ヴィンテージの見た目：3つのゲームのホーム・マイページ・ルールと設定・レート戦のダイアログ・3つのゲームの対局中（七並べのボタンは1段のまま）。
- **まだ確かめていないこと（本物のスマホで、ユーザーに試してもらう）**：
  - スピードのドラッグとはじきの手ざわり（はじきの判定の速さ0.45px/ms・向き32度は仮の値）。ドラッグになって AI が強く感じるなら「つよい」を少しゆっくりにする。
  - スマホ2台でのスピードの対人戦（通信の遅れの感じ方）。
  - Android／PC の Chrome のインストール画面（Browserペインは Claude アプリなので出ない）。
  - iPhone での BGM（最初のタップで鳴り始めるか・音量・マナーモードのとき）。
  - すでにホーム画面に追加してあるアイコンの名前は「大富豪」のまま（消して追加し直すと「Cards Table」）。アイコンの絵は王冠とカードのまま。
  - スピードで、相手がつながっているのに出せるカードを出さないと、自分も出せないときは止まったままになる（「せーの」は2人とも出せないときだけ）。
  - 本物の iPhone でのロビーの絨毯の見え方（模様の大きさ・明るさ）。オンラインの待合室（部屋に入ったあと）は手元サーバーでまだ見ていない（入口の画面は確認済み）。
  - 本物の iPhone での額縁の見え方（線の細さ・角の飾りの大きさ）と Kaisei Decol の読みやすさ。額縁の太さは CSS の `border-image` の幅（8px など）で変えられる。

## 次の候補・改善点（未決定。ユーザーと相談して決める）

2026-09-26（4）に批判的に見直した結果（ユーザーに伝えた順）：
1. **保存がこの端末のブラウザだけ**：AI戦のレート・途中の対局・オンラインの本人ID（`daifugo.cid`）は localStorage。iPhoneではSafariとホーム画面のアプリで保存場所が別、Safariで7日ほど開かないと消えることがある、機種変更で引き継げない。→「引き継ぎコード」（書き出し・読み込み。無料でできる）。`navigator.storage.persist()` も。
2. **大富豪の「つよい」も時間で考える量が決まる**（`js/ai.js` の `chooseTurnMC`、420ms）：スマホでは弱くなり、レート1630とずれる。七並べと同じく「同じ配り方で比べる＋回数で固定」にして測り直す（大富豪は1試合が長いので測るのに時間がかかる）。
3. **3つのゲームの画面の進行がほぼコピー**（メニュー・結果・棄権・オンラインの結果）。4つ目のゲームを足す前に共通化しないと、直し漏れが増える。
4. **テストの自動実行**：GitHub Actions（公開リポジトリは無料）で push のたびに `node tools/run_tests.js`。新しいサービスなので相談してから。
5. レート戦で「ヒント」（ふつうAIのおすすめ）が使える。レート戦では消すか相談。
6. オンラインのレートは1人で2つのブラウザを使えば自分に得点を集められる（友だち同士なら問題ない）。
7. オンラインのスピードは通信の速い人が少し有利（サーバーに先に届いた方が出せる）。
- ほかの候補：新しいゲーム（ポーカー＝テキサスホールデム・チップは架空のみ、ババ抜き・ダウト・神経衰弱・ブラックジャック）／1手ごとの制限時間（オンライン）／スタンプ（絵）の送信／観戦モード、戦績の記録／アイコンを「Cards Table」らしい絵に
